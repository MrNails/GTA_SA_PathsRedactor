<UserControl x:Class="GTA_SA_PathsRedactor.View.UserControls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GTA_SA_PathsRedactor.View.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="300"
             Name="Root">
    <UserControl.Resources>
        <Style x:Key="NumericButtonStyle"
               TargetType="Button">
            <Setter Property="Padding" Value="8 3"/>
            <Setter Property="FontSize" Value="15" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <Button Tag="{x:Static local:SignType.Minus}"
                Content="-" 
                Style="{StaticResource NumericButtonStyle}"
                DockPanel.Dock="Left"
                PreviewMouseDown="NumericButtonOnMouseDown"
                PreviewMouseUp="NumericButtonOnMouseUp"/>
        <Button Tag="{x:Static local:SignType.Plus}"
                Content="+" 
                Style="{StaticResource NumericButtonStyle}"
                DockPanel.Dock="Right"
                PreviewMouseDown="NumericButtonOnMouseDown"
                PreviewMouseUp="NumericButtonOnMouseUp"/>
        <TextBox MaxHeight="28"
                 Margin="5 0 5 0"
                 Text="{Binding ElementName=Root, Path=Value, Mode=TwoWay, Delay=300}" 
                 PreviewTextInput="ValueField_OnTextInput"/>
    </DockPanel>
</UserControl>
